[pytest]
markers =
    smoke: Quick critical-path tests that run on every PR. Should complete in seconds.
    regression: Full regression suite. Runs nightly or on release branches.
    critical_path: Tests that block release if they fail. Independent of speed.
    slow: Tests that take noticeable time (network, retries, waits). Excluded from PR builds.