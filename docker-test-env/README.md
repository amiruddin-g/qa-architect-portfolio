# Docker Test Environment

> 🚧 In progress. Skeleton committed; content being built incrementally.

Containerized test environments for reproducible CI runs — same image locally, in CI, and (eventually) for parallel execution.

## What this module will demonstrate

- A `Dockerfile` that produces a clean, reproducible test environment
- A `docker-compose.yml` for orchestrating dependencies (e.g. a database, a mock service) alongside the test runner
- Running the existing `test-tagging/` suite inside the container
- Notes on image size, layer caching, and tradeoffs

## Why containerized tests matter

(coming soon — to be written when the first working version is committed)

## How to run

(coming soon)

---

*Part of [qa-architect-portfolio](../).*